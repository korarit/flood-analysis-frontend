# UI Requirements — Water Situation Platform

## 1. ภาพรวมระบบ

ระบบ Web Platform สำหรับติดตามและสรุปสถานการณ์น้ำ โดยรองรับหลายลุ่มน้ำภายใต้ Platform เดียวกัน

แนวคิดหลัก:

* ระบบรองรับหลายลุ่มน้ำ
* ผู้ใช้เลือก **ลุ่มน้ำ** เพื่อกำหนดขอบเขตข้อมูลที่ต้องการดู
* UI และ Component ใช้ร่วมกันทุกลุ่มน้ำ
* ข้อมูลสถานีแบ่งเป็น

  * สถานีวัดระดับน้ำ
  * สถานีวัดปริมาณน้ำฝน
* ผู้ใช้สามารถกำหนด **สถานีใกล้ฉัน** ได้ 1 สถานี
* สถานีใกล้ฉันใช้เป็นจุดอ้างอิงหลักสำหรับผู้ใช้ในการติดตามสถานการณ์น้ำในพื้นที่
* รายการสถานีใกล้ฉันเก็บใน Local Storage
* ระบบไม่จัดเก็บตำแหน่งของผู้ใช้
* Historical data แสดงย้อนหลังสูงสุด 7 วัน
* UI ต้องออกแบบให้รองรับการเพิ่มลุ่มน้ำในอนาคตโดยไม่ต้องสร้าง UI ใหม่สำหรับแต่ละลุ่มน้ำ

---

# 2. Information Architecture

โครงสร้างหลักของระบบ:

```text
Water Situation Platform
│
├── Basin Selection
│
└── Basin
    │
    ├── Overview
    │
    ├── Map
    │
    ├── Station
    │   └── Station Detail
    │
    └── [Future]
        ├── Event
        ├── Alert
        └── Report
```

URL หลัก:

```text
/basin/{name-eng}
/basin/{name-eng}/map
/basin/{name-eng}/station
/basin/{name-eng}/station/{id}
```

ตัวอย่าง:

```text
/basin/yom
/basin/yom/map
/basin/yom/station
/basin/yom/station/Y-0014
```

---

# 3. Global Navigation

Navigation ต้องสามารถเข้าถึงฟังก์ชันหลักได้จากทุกหน้าของ Basin

ตัวอย่าง:

```text
┌─────────────────────────────────────────────────────────┐
│ Water Situation        ลุ่มน้ำยม ▼       🔍 ค้นหาสถานี │
├─────────────────────────────────────────────────────────┤
│ ภาพรวม │ แผนที่ │ สถานี │                             │
└─────────────────────────────────────────────────────────┘
```

เมนูหลัก:

* ภาพรวม
* แผนที่
* สถานี

เมนูในอนาคต:

* เหตุการณ์
* การแจ้งเตือน
* รายงาน

---

# 4. Basin Selection

## 4.1 หน้าที่

ให้ผู้ใช้เลือกลุ่มน้ำที่ต้องการติดตาม

ระบบต้องไม่ผูก UI กับลุ่มน้ำยมโดยเฉพาะ

ตัวอย่าง:

```text
เลือกลุ่มน้ำ

┌─────────────────────────────┐
│ ลุ่มน้ำยม                   │
│ Yom Basin                   │
│                             │
│ สถานีทั้งหมด 42 สถานี       │
│                             │
│              [ดูสถานการณ์] │
└─────────────────────────────┘

┌─────────────────────────────┐
│ ลุ่มน้ำน่าน                 │
│ Nan Basin                   │
│                             │
│ สถานีทั้งหมด 65 สถานี       │
│                             │
│              [ดูสถานการณ์] │
└─────────────────────────────┘
```

## 4.2 Requirements

* แสดงเฉพาะลุ่มน้ำที่ระบบเปิดใช้งาน
* แสดงชื่อภาษาไทย
* แสดงชื่อภาษาอังกฤษหากต้องการ
* สามารถค้นหาลุ่มน้ำได้ หากจำนวนลุ่มน้ำเพิ่มขึ้นมาก
* คลิกแล้วเข้าสู่:

```text
/basin/{slug}
```

* หากไม่มี Basin ที่ต้องการ ต้องแสดง Empty State

---

# 5. Basin Context

เมื่อผู้ใช้เข้ามาที่:

```text
/basin/yom
```

ระบบต้องถือว่า:

```text
Current Basin = Yom
```

ทุกหน้าภายใต้ Basin ต้องใช้ Basin เดียวกัน

เช่น:

```text
/basin/yom
/basin/yom/map
/basin/yom/station
```

หากเปลี่ยนเป็นลุ่มน้ำน่าน:

```text
/basin/nan
/basin/nan/map
/basin/nan/station
```

UI Component เดิมทั้งหมด แต่ข้อมูลเปลี่ยนตาม Basin

---

# 6. Basin Overview

URL:

```text
/basin/{name-eng}
```

เป็นหน้าแรกของลุ่มน้ำ

จุดประสงค์คือให้ผู้ใช้สามารถ **เข้าใจสถานการณ์ของลุ่มน้ำได้อย่างรวดเร็ว โดยไม่ต้องเปิดดูทีละสถานี**

---

## 6.1 Basin Header

แสดง:

```text
ลุ่มน้ำยม
Yom Basin

ข้อมูลล่าสุด 18:05 น.
```

อาจมี:

* วันที่
* เวลาที่ข้อมูลล่าสุด
* สถานะข้อมูล

---

# 7. Situation Summary

ด้านบนของหน้า Overview ควรมี Summary Card

ตัวอย่าง:

```text
┌─────────────────────────────────────────┐
│ สถานการณ์ลุ่มน้ำยม                     │
│                                         │
│ 🟠 เฝ้าระวัง                            │
│                                         │
│ สถานีระดับน้ำเฝ้าระวัง       6 สถานี   │
│ สถานีระดับน้ำเพิ่มขึ้น       9 สถานี   │
│ สถานีฝนตกหนัก                4 สถานี   │
│                                         │
│ อัปเดตล่าสุด 18:05 น.                  │
└─────────────────────────────────────────┘
```

สถานะอาจแบ่งเป็น:

```text
🟢 ปกติ
🟡 เฝ้าระวัง
🟠 ระวัง
🔴 วิกฤต
```

> สีเป็นเพียงตัวช่วยในการสื่อสาร ต้องมีข้อความ/ไอคอนประกอบเสมอ

---

# 8. สถานีใกล้ฉัน

เป็น Feature สำคัญของระบบ

## 8.1 แนวคิด

ผู้ใช้ทั่วไปมักต้องการตอบคำถาม:

> "น้ำจะท่วมบ้านฉันไหม?"

ดังนั้นผู้ใช้สามารถเลือก **สถานีวัดระดับน้ำใกล้ฉัน 1 สถานี** เพื่อใช้เป็นจุดอ้างอิง

ไม่ใช่ระบบ Favorite หลายสถานี

---

# 9. Nearby Station UI

หากยังไม่เคยเลือก:

```text
┌──────────────────────────────────────┐
│ 📍 สถานีใกล้ฉัน                      │
│                                      │
│ เลือกสถานีวัดระดับน้ำที่อยู่ใกล้     │
│ พื้นที่ของคุณ เพื่อใช้ติดตาม         │
│ สถานการณ์น้ำ                         │
│                                      │
│          [ เลือกสถานีใกล้ฉัน ]      │
└──────────────────────────────────────┘
```

---

# 10. Nearby Station Selection

เมื่อผู้ใช้กด:

```text
[ เลือกสถานีใกล้ฉัน ]
```

ระบบขอ Location Permission จาก Browser

จากนั้นแสดงสถานีวัดระดับน้ำใกล้เคียง

ตัวอย่าง:

```text
สถานีวัดระดับน้ำใกล้คุณ

┌──────────────────────────────┐
│ 🌊 Y.14 ศรีสัชนาลัย          │
│                              │
│ ระยะทาง 3.2 km              │
│ ระดับน้ำ 5.82 m             │
│ Q 285 m³/s                  │
│                              │
│           [เลือกสถานีนี้]   │
└──────────────────────────────┘
```

สามารถแสดงหลายสถานีให้ผู้ใช้เลือกได้ แต่ **หลังจากเลือกแล้วจะเก็บเพียง 1 สถานี**

---

# 11. Nearby Station หลังเลือกแล้ว

```text
┌──────────────────────────────────────┐
│ 📍 สถานีใกล้ฉัน                      │
│                                      │
│ Y.14 ศรีสัชนาลัย                    │
│ ห่างจากตำแหน่งที่เลือก 3.2 km       │
│                                      │
│ ระดับน้ำ                             │
│ 5.82 m ↑                             │
│                                      │
│ ปริมาณน้ำ                            │
│ 285 m³/s                             │
│                                      │
│ 🟠 เฝ้าระวัง                         │
│                                      │
│ [ดูรายละเอียด]     [เปลี่ยนสถานี]  │
└──────────────────────────────────────┘
```

---

# 12. Nearby Station Local Storage

UI ต้องเก็บเพียง:

```json
{
  "nearbyStationId": "Y-0014"
}
```

ข้อกำหนด:

* มีสถานีใกล้ฉันได้เพียง 1 สถานี
* การเลือกใหม่ต้องแทนค่าของเดิม
* ไม่ต้องเก็บข้อมูลสถานีซ้ำ
* ไม่ต้องเก็บพิกัดผู้ใช้
* ไม่ต้องเก็บประวัติตำแหน่ง
* เมื่อโหลดหน้าใหม่ให้ใช้ `station_id` นี้เรียกข้อมูลสถานีจาก API

หาก Local Storage ไม่มี station:

```text
แสดง UI สำหรับเลือกสถานี
```

หากมี:

```text
แสดงสถานีใกล้ฉัน
```

---

# 13. กรณี Location Permission ถูกปฏิเสธ

ต้องไม่ทำให้ผู้ใช้ใช้ระบบต่อไม่ได้

แสดง:

```text
ไม่สามารถเข้าถึงตำแหน่งของคุณได้

คุณยังสามารถค้นหาและเลือกสถานี
จากแผนที่หรือรายการสถานีได้

[ค้นหาสถานี]
[ดูแผนที่]
```

---

# 14. Station Search

ระบบใช้คำว่า:

> **ค้นหาสถานี**

ไม่ใช้ "ค้นหาสถานีระดับน้ำ" หรือ "ค้นหาสถานีฝน"

เพราะ Search เดียวรองรับสถานีทุกประเภท

---

# 15. Search UI

```text
┌────────────────────────────────────────────┐
│ 🔍 ค้นหาสถานี                             │
│ [ ศรีสัชนาลัย________________________ ]    │
└────────────────────────────────────────────┘
```

สามารถค้นหาจาก:

* ชื่อสถานี
* รหัสสถานี
* พื้นที่
* ที่อยู่
* ชื่อหน่วยงาน

---

# 16. Station Filter

ใต้ Search:

```text
[ ทั้งหมด ] [ วัดระดับน้ำ ] [ วัดปริมาณน้ำฝน ]
```

Default:

```text
ทั้งหมด
```

## 16.1 ทั้งหมด

แสดง:

```text
🌊 สถานีวัดระดับน้ำ
🌧️ สถานีวัดปริมาณน้ำฝน
```

## 16.2 วัดระดับน้ำ

แสดงเฉพาะ:

```text
🌊 Water Level Station
```

## 16.3 วัดปริมาณน้ำฝน

แสดงเฉพาะ:

```text
🌧️ Rainfall Station
```

---

# 17. Station List

URL:

```text
/basin/{name-eng}/station
```

ตัวอย่าง:

```text
/basin/yom/station
```

Layout:

```text
ค้นหาสถานี

[___________________________]

[ ทั้งหมด ] [ วัดระดับน้ำ ] [ วัดปริมาณน้ำฝน ]

สถานีทั้งหมด 42 สถานี

┌─────────────────────────────┐
│ 🌊 Y.14 ศรีสัชนาลัย         │
│ สถานีวัดระดับน้ำ            │
│ อ.ศรีสัชนาลัย จ.สุโขทัย     │
│ ระดับน้ำ 5.82 m             │
│ อัปเดต 18:05 น.             │
└─────────────────────────────┘
```

---

# 18. Station Card

แต่ละ Card ควรแสดงข้อมูลสั้น ๆ:

### Common

* Icon ประเภทสถานี
* Station ID
* Station Name
* Address
* Agency
* Last update

### Water Level

* ระดับน้ำ
* Q (m³/s) ถ้ามี
* Trend
* Status

### Rainfall

* ปริมาณฝน
* ช่วงเวลาของข้อมูล
* Trend หากมี
* Status

---

# 19. Station Detail

URL:

```text
/basin/{name-eng}/station/{id}
```

ตัวอย่าง:

```text
/basin/yom/station/Y-0014
```

---

# 20. Station Detail Header

```text
🌊 สถานีวัดระดับน้ำ

Y.14
ศรีสัชนาลัย

อ.ศรีสัชนาลัย จ.สุโขทัย

หน่วยงาน
[ชื่อหน่วยงานเต็ม]

[ดูบนแผนที่]
```

---

# 21. Water Level Station Detail

แสดง Current Data เป็นข้อมูลหลัก

```text
┌──────────────────┐
│ ระดับน้ำ         │
│                  │
│ 5.82 m           │
│ ↑ 0.24 m         │
└──────────────────┘

┌──────────────────┐
│ ปริมาณน้ำ        │
│                  │
│ 285 m³/s         │
└──────────────────┘
```

และ:

```text
ข้อมูลล่าสุด
22 ส.ค. 2026
18:05 น.
```

---

# 22. Rainfall Station Detail

```text
🌧️ สถานีวัดปริมาณน้ำฝน

ชื่อสถานี

ฝนล่าสุด
42.5 mm

ช่วงข้อมูล
1 ชั่วโมง

ข้อมูลล่าสุด
18:00 น.
```

---

# 23. Historical Data

Historical data แสดงย้อนหลังสูงสุด:

```text
7 วัน
```

ตัวเลือก:

```text
[24 ชั่วโมง] [3 วัน] [7 วัน]
```

ไม่ควรมี UI ให้เลือกย้อนหลังเกิน 7 วันในระยะแรก

---

# 24. Historical Chart — Water Level

กราฟสามารถมี:

```text
ระดับน้ำ (m)
│
│        ╭──╮
│    ╭───╯  ╰──╮
│────╯         ╰────
└────────────────────
  00  06  12  18
```

หากมี Q:

```text
Series:
☑ ระดับน้ำ
☑ ปริมาณน้ำ
```

ควรแยกแกนเมื่อหน่วยต่างกันมาก

---

# 25. Historical Chart — Rainfall

แสดงเป็นกราฟแท่งหรือรูปแบบที่เหมาะกับ rainfall

```text
ฝน (mm)

│       █
│       █
│   █   █
│   █   █       █
└──────────────────
  00  06  12  18
```

แสดง timestamp ชัดเจน

---

# 26. Data Gap

หากไม่มีข้อมูลช่วงหนึ่ง ห้ามลากเส้นให้ดูเหมือนมีข้อมูลต่อเนื่อง

ควรแสดง:

```text
ไม่มีข้อมูล
```

หรือ gap ในกราฟ

และอาจแสดง:

```text
⚠ ข้อมูลขาดหาย 13:00–14:00 น.
```

---

# 27. Station Relationship UI

ใน Station Detail สามารถมี section:

```text
ความสัมพันธ์ของสถานี
```

สำหรับสถานีระดับน้ำ:

```text
🌧️ สถานีฝนที่เกี่ยวข้อง

P-001 บ้าน...
P-002 ...
P-003 ...
```

สำหรับสถานีฝน:

```text
🌊 สถานีระดับน้ำที่เกี่ยวข้อง

Y-0014
Y-0015
Y-0016
```

หากยังไม่มีข้อมูล:

```text
ยังไม่มีข้อมูลความสัมพันธ์ของสถานี
```

---

# 28. Map

URL:

```text
/basin/{name-eng}/map
```

ตัวอย่าง:

```text
/basin/yom/map
```

Map เป็นหนึ่งในหน้าหลักของระบบ

---

# 29. Map Layout

Desktop:

```text
┌───────────────┬───────────────────────────────┐
│               │                               │
│ Station       │                               │
│ Search        │                               │
│               │             MAP               │
│ Filter        │                               │
│               │                               │
│ Station List  │                               │
│               │                               │
└───────────────┴───────────────────────────────┘
```

Mobile:

```text
┌───────────────────────────┐
│ 🔍 ค้นหาสถานี             │
├───────────────────────────┤
│                           │
│           MAP             │
│                           │
│                           │
├───────────────────────────┤
│ [รายการสถานี]             │
└───────────────────────────┘
```

---

# 30. Map Layers

## Base Map

* Street Map
* Satellite หากมี

## Hydrology

* แม่น้ำ
* ลำน้ำ
* Drainage Network
* Basin boundary
* Catchment boundary หากมี

## Station

* สถานีวัดระดับน้ำ
* สถานีวัดปริมาณน้ำฝน

## Situation

* สถานีปกติ
* เฝ้าระวัง
* แจ้งเตือน
* ข้อมูลขาดหาย

---

# 31. Station Marker

Water Level:

```text
🌊
Y.14
```

Rainfall:

```text
🌧️
P-001
```

เมื่อกด Marker:

```text
┌──────────────────────────────┐
│ 🌊 Y.14                      │
│ ศรีสัชนาลัย                  │
│                              │
│ ระดับน้ำ 5.82 m              │
│ Q 285 m³/s                   │
│ 🟠 เฝ้าระวัง                 │
│                              │
│ [รายละเอียด]                │
└──────────────────────────────┘
```

---

# 32. Map Filter

ควรมี:

```text
สถานี

☑ วัดระดับน้ำ
☑ วัดปริมาณน้ำฝน
```

และ:

```text
สถานการณ์

☑ ปกติ
☑ เฝ้าระวัง
☑ แจ้งเตือน
☑ ข้อมูลขาดหาย
```

---

# 33. River View

ในอนาคต/หน้า Overview สามารถแสดงสถานีเรียงตามแนวแม่น้ำ

สำหรับแม่น้ำยม:

```text
ต้นน้ำ
  │
  ▼
[Station]
  │
[Station]
  │
[Station]
  │
[Station]
  │
  ▼
ปลายน้ำ
```

แต่ UI ต้องสร้างจากข้อมูลของ Basin/River ไม่ hard-code จังหวัด

---

# 34. Rainfall Overview

ในหน้า Basin Overview ควรมีส่วน:

```text
ปริมาณน้ำฝน

สถานีฝนที่มีปริมาณสูง

🌧️ P-001    85 mm
🌧️ P-002    72 mm
🌧️ P-003    65 mm
```

อาจเลือกช่วง:

```text
1 ชั่วโมง
3 ชั่วโมง
6 ชั่วโมง
24 ชั่วโมง
```

เฉพาะช่วงที่ Backend มีข้อมูลรองรับ

---

# 35. Water Level Overview

ควรมี:

```text
สถานีระดับน้ำ

สถานีที่ระดับน้ำสูง
────────────────────

Y.14   5.82 m   🟠
Y.15   5.21 m   🟡
Y.16   4.82 m   🟢
```

สามารถเรียงตาม:

* ระดับน้ำ
* การเปลี่ยนแปลง
* ตำแหน่งตามแม่น้ำ

---

# 36. Data Status

ทุกข้อมูลที่มี timestamp ควรแสดงเวลา

ตัวอย่าง:

```text
ข้อมูลล่าสุด 18:05 น.
```

หากข้อมูลล่าช้า:

```text
🟡 ข้อมูลล่าช้า
ข้อมูลล่าสุด 15:00 น.
```

หากข้อมูลขาดหาย:

```text
🔴 ไม่มีข้อมูลล่าสุด
```

ผู้ใช้ต้องสามารถแยกได้ว่า:

> "ระดับน้ำไม่มีการเปลี่ยนแปลง"

กับ

> "ไม่มีข้อมูลใหม่"

---

# 37. Event UI

ระบบควรเผื่อพื้นที่สำหรับเหตุการณ์:

```text
เหตุการณ์ล่าสุด

🟠 ระดับน้ำเพิ่มขึ้นเร็ว
Y.14 ศรีสัชนาลัย
18:02 น.

🌧️ ฝนตกหนัก
พื้นที่ต้นน้ำ
17:45 น.
```

---

# 38. Alert UI

ตัวอย่าง:

```text
🔴 แจ้งเตือน

สถานี Y.14 ศรีสัชนาลัย

ระดับน้ำอยู่ในระดับแจ้งเตือน

18:05 น.
```

หรือ:

```text
🟠 เฝ้าระวัง

มีสถานีระดับน้ำเพิ่มขึ้น
อย่างต่อเนื่องในพื้นที่
```

รายละเอียด rule ไม่ควรแสดงจนทำให้ผู้ใช้ทั่วไปสับสน แต่ควรมีรายละเอียดให้ผู้ใช้เปิดดูได้

---

# 39. Mobile UX

เนื่องจากระบบเกี่ยวข้องกับสถานการณ์น้ำ ผู้ใช้สามารถเข้าจากมือถือในสถานการณ์ฉุกเฉินได้

หน้า Mobile ต้องให้ความสำคัญกับ:

1. สถานีใกล้ฉัน
2. ระดับน้ำปัจจุบัน
3. สถานะ
4. เวลาอัปเดต
5. แผนที่
6. ค้นหาสถานี

ตัวอย่าง:

```text
┌─────────────────────┐
│ ลุ่มน้ำยม           │
├─────────────────────┤
│ 📍 สถานีใกล้ฉัน      │
│                     │
│ Y.14                │
│ 5.82 m              │
│ 🟠 เฝ้าระวัง         │
│                     │
│ [รายละเอียด]        │
├─────────────────────┤
│ สถานการณ์ลุ่มน้ำ    │
│ 🟠 เฝ้าระวัง         │
├─────────────────────┤
│ 🗺 แผนที่             │
├─────────────────────┤
│ 🔍 ค้นหาสถานี        │
└─────────────────────┘
```

---

# 40. Loading State

ทุกหน้า/Component ที่โหลดข้อมูลต้องมี Loading State

เช่น:

```text
กำลังโหลดข้อมูลสถานี...
```

หรือ Skeleton UI

ไม่ควรแสดงค่า `0` แทนข้อมูลที่ยังโหลดไม่เสร็จ เพราะอาจทำให้เข้าใจผิดว่าไม่มีสถานี/ไม่มีน้ำ

---

# 41. Empty State

ตัวอย่าง:

### ไม่มีสถานี

```text
ไม่พบสถานี
ลองเปลี่ยนคำค้นหาหรือประเภทสถานี
```

### ไม่มีข้อมูลย้อนหลัง

```text
ยังไม่มีข้อมูลย้อนหลังสำหรับสถานีนี้
```

### ไม่มี Relationship

```text
ยังไม่มีข้อมูลความสัมพันธ์ของสถานี
```

---

# 42. Error State

ตัวอย่าง:

```text
ไม่สามารถโหลดข้อมูลสถานีได้

กรุณาลองใหม่อีกครั้ง

[ลองใหม่]
```

หาก Map โหลดไม่ได้:

```text
ไม่สามารถโหลดแผนที่ได้

[ลองใหม่]
```

ไม่ควรปล่อยหน้าว่างโดยไม่มีคำอธิบาย

---

# 43. Responsive Navigation

Desktop อาจใช้ Sidebar หรือ Top Navigation

Mobile ควรใช้ Bottom Navigation หรือ Compact Navigation

ตัวอย่าง:

```text
┌─────────────────────┐
│                     │
│       Content       │
│                     │
├─────────────────────┤
│ ภาพรวม │ แผนที่ │ สถานี │
└─────────────────────┘
```

---

# 44. Internationalization

ข้อมูล Station มีทั้ง:

* Thai
* English

UI ควรรองรับ locale

ตัวอย่าง:

```text
ภาษาไทย

ศรีสัชนาลัย
อำเภอศรีสัชนาลัย จังหวัดสุโขทัย
```

และ:

```text
English

Si Satchanalai
Si Satchanalai District, Sukhothai
```

แต่ชื่อข้อมูลจากหน่วยงานต้องไม่แปลเองโดย Frontend หาก Backend มีชื่อ Thai/English จาก Source อยู่แล้ว

---

# 45. Units

หน่วยต้องแสดงชัดเจน:

| Data        | Unit |
| ----------- | ---- |
| Water Level | m    |
| Discharge   | m³/s |
| Rainfall    | mm   |
| Distance    | km   |

ไม่ควรแสดงตัวเลขโดยไม่มีหน่วย

---

# 46. Accessibility

UI ต้องไม่ใช้สีเป็นตัวบอกสถานการณ์เพียงอย่างเดียว

ตัวอย่างที่ไม่ควรทำ:

```text
●
```

โดยไม่บอกว่าเป็นอะไร

ควรเป็น:

```text
🟠 เฝ้าระวัง
```

รองรับ:

* Keyboard navigation
* Screen reader
* Accessible label
* Contrast
* Focus state
* Tooltip ที่เข้าถึงได้

---

# 47. Performance Requirements

UI ต้องหลีกเลี่ยงการโหลดข้อมูลเกินความจำเป็น

### Station List

โหลด:

```text
Station metadata
+
Latest observation
```

ไม่โหลด:

```text
7-day history
```

### Station Detail

โหลด:

```text
Metadata
+
Current data
```

แล้วค่อยโหลด:

```text
History
Relationship
```

เมื่อจำเป็น

### Map

โหลด:

```text
Coordinates
+
Station metadata
+
Current status
```

ไม่โหลด historical data ทั้งหมด

---

# 48. สิ่งที่ UI ไม่ควรทำ

Frontend **ไม่ควร**:

* คำนวณ Hydrological Model
* สร้าง DEM
* สร้าง Watershed
* คำนวณ Drainage Network
* คำนวณ Station Relationship จาก raw spatial data
* ยิง ThaiWater API โดยตรง
* เก็บ historical data จำนวนมากใน Local Storage
* เก็บตำแหน่งผู้ใช้บน Server
* สร้าง UI แยกสำหรับแต่ละ Basin

---

# 49. Future UI Features

โครงสร้าง UI ควรเผื่อ:

```text
/basin/{slug}/event
/basin/{slug}/alert
/basin/{slug}/report
```

และสามารถเพิ่ม:

* Flood Forecast
* Rainfall Forecast
* Flood Map
* Situation Timeline
* River Profile
* Notification
* Report Generation

โดยไม่เปลี่ยนโครงสร้างหลักของ Platform

---

# 50. UI Principle สรุป

ระบบควรยึดหลัก:

> **เลือก Basin → ดูภาพรวม → ดูสถานีใกล้ฉัน → ดูแผนที่ → ค้นหาสถานี → ดูรายละเอียดสถานี**

และสำหรับผู้ใช้ทั่วไป เส้นทางที่สำคัญที่สุดคือ:

```text
เลือก "ลุ่มน้ำยม"
       ↓
ภาพรวมสถานการณ์
       ↓
📍 สถานีใกล้ฉัน
       ↓
Y.14 ศรีสัชนาลัย
       ↓
ระดับน้ำ 5.82 m
Q 285 m³/s
🟠 เฝ้าระวัง
       ↓
ดูกราฟย้อนหลัง 7 วัน
```

ขณะที่ผู้ใช้ที่ต้องการวิเคราะห์พื้นที่จะใช้:

```text
เลือก Basin
   ↓
Map
   ↓
ค้นหาสถานี
   ↓
Filter
├── ทั้งหมด
├── วัดระดับน้ำ
└── วัดปริมาณน้ำฝน
   ↓
Station Detail
   ↓
History / Relationship
```

นี่จะทำให้ UI รองรับทั้ง **ประชาชนทั่วไปที่อยากรู้ว่าน้ำใกล้บ้านเป็นอย่างไร** และ **ผู้ใช้ที่ต้องการสำรวจสถานการณ์ทั้งลุ่มน้ำ** โดยใช้โครงสร้างเดียวกันครับ
